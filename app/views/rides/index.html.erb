<div class="jumbotron">
  <div class="container">
    <h1>Nearby Rides</h1>
    <% @rides.each do |ride| %>
      <li><%= ride.address %> to <%= ride.destination %></li>
    <% end %>
  </div>
</div>
